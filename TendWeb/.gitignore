node_modules/
dist/
.env
.env.local
*.local

# Scratch output from manual smoke-test runs (scripts/smoke*.mjs default to
# writing a screenshot alongside wherever they're invoked from unless SHOT is
# set) — never something to commit.
*.png
